import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import SmartSelectDropdown from './SmartSelectDropdown';

describe('<SmartSelectDropdown />', () => {
  /* test('it should mount', () => {
    render(<SmartSelectDropdown />);
    
    const smartSelectDropdown = screen.getByTestId('SmartSelectDropdown');

    expect(smartSelectDropdown).toBeInTheDocument();
  }); */
});