import React from 'react';
import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import SmartSelectInputBox from './SmartSelectInputBox';

describe('<SmartSelectInputBox />', () => {
  /* test('it should mount', () => {
    render(<SmartSelectInputBox />);
    
    const smartSelectInputBox = screen.getByTestId('SmartSelectInputBox');

    expect(smartSelectInputBox).toBeInTheDocument();
  }); */
});